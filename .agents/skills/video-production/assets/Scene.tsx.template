import React from "react";
import {Audio} from "@remotion/media";
import {staticFile, useCurrentFrame, useVideoConfig} from "remotion";
import timestamps from __TIMESTAMPS__;
import {WordCaptions} from "../WordCaptions";

// Wiring placeholder, NOT art direction or a deliverable scene.
// Replace the visible treatment with an original composition from the directorial brief:
// footage, illustration, object animation, motion graphics, or a purposeful combination.
// Keep audio and captions synchronized; choose styling and staging for this project.
export const Scene__SCENE__: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, width, height} = useVideoConfig();
  return <div style={{width, height, background: "#171717", color: "#ffffff", position: "relative", fontFamily: "sans-serif"}}>
    <Audio src={staticFile(__AUDIO__)} />
    <div style={{padding: 64, fontSize: 36}}>
      {"Unfinished scene: " + __TITLE__}
    </div>
    <WordCaptions words={timestamps.words} time={frame / fps} />
  </div>;
};
