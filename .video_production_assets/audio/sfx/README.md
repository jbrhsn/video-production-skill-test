# Sound effects

Store one-shots and ambience here. Record the effect's action, duration, source/rights, peak level, and best usage (for example, `UI tap`, `paper reveal`, or `soft transition`) in `library.json`.
