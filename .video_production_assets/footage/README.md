# Footage

Keep raw or licensed B-roll and screen recordings here. Add each usable clip to `library.json` with its source, permission basis, duration, aspect ratio, visible action, and whether its original audio may be used.

Use `reference-only` for clips saved to study pacing or composition; they must never be rendered into an output.
