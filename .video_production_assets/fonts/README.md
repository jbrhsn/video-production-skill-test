# Fonts

Place only fonts whose use and distribution terms are understood. Keep a license text or provider URL beside each family, and record the allowed use in `library.json`. A font being installed locally does not make it safe to package or publish.
